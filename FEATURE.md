# Xray Core 特性说明

本文档说明当前仓库中针对 Socks5 动态出口做的几项特殊增强：

- `{sid-N}` 动态字母数字 ID
- `{did-N}` 动态纯数字 ID
- `{kp-N}` 动态 ID 保活
- 来源 IP 触发的用户级 Socks5 出站隔离
- 按出站国家预验证的动态账号缓冲池

## 1. 适用范围

这些增强目前作用在 `socks` 出站账号上，也就是 Socks5 出站配置里的用户名和密码模板。

典型使用场景是住宅代理、动态会话出口、按用户来源 IP 固定分配出口身份。

## 2. 动态占位符

### 2.1 `{sid-N}`

含义：生成长度为 `N` 的字母数字随机串。

示例：

```text
brd-zone-us-session-{sid-8}
```

可能生成：

```text
brd-zone-us-session-A1b2C3d4
```

特点：

- 字符集为大小写字母加数字
- 每次普通动态生成时都可能不同
- 可用于用户名，也可用于密码

### 2.2 `{did-N}`

含义：生成长度为 `N` 的纯数字随机串。

示例：

```text
brd-zone-us-session-{did-10}
```

可能生成：

```text
brd-zone-us-session-4829517306
```

特点：

- 仅包含数字 `0-9`
- 每次普通动态生成时都可能不同
- 可用于用户名，也可用于密码

### 2.3 `{kp-N}`

含义：为动态生成结果增加保活时间，单位为秒。

示例：

```text
brd-zone-us-session-{sid-8}{kp-30}
```

行为：

- 第一次生成后会缓存结果
- 在 `30` 秒内再次使用同一模板时，会复用同一个动态值
- 超过保活时间后，再次生成会得到新的动态值

说明：

- `{kp-N}` 本身不生成 ID，它只控制 `{sid-N}` 或 `{did-N}` 的复用时间
- 缓存键基于模板内容，不包含客户端来源信息

## 3. 用户级 Socks5 出站隔离

### 3.1 目标

当客户端通过某个入站连接到服务器时，如果该连接最终路由到一个带动态账号模板的 Socks5 出站，那么系统会根据客户端来源 IP，为该客户端固定分配一个动态出口 ID。

这样可以实现：

- 同一个来源 IP 多次连接时，使用同一个 Socks5 动态出口身份
- 不同来源 IP 使用不同的 Socks5 动态出口身份

这适合“一个用户一个固定住宅出口”的场景。

### 3.2 生效条件

只有同时满足下面条件时，才会启用用户级绑定逻辑：

1. 该流量最终命中的出站必须是 `socks`
2. 该 Socks5 出站账号的用户名或密码中，必须包含 `{sid-N}` 或 `{did-N}`
3. 入站上下文里必须能拿到客户端来源 IP

只要有一项不满足，就不会触发用户级绑定，仍然走原有逻辑。

### 3.3 绑定规则

绑定粒度为：

```text
客户端来源 IP + 出站作用域
```

当前实现中，出站作用域优先使用：

```text
outbound tag
```

如果取不到 tag，则回退为：

```text
socks 服务器地址
```

这意味着：

- 同一个客户端 IP 访问同一个目标 Socks5 出站时，会复用同一个动态 ID
- 同一个客户端 IP 如果走不同 Socks5 出站，会得到不同动态 ID
- 不同客户端 IP 即使走同一个 Socks5 出站，也会得到不同动态 ID
- 绑定缓存按最后访问时间保活，某个来源 IP 超过 5 小时没有再访问同一出站作用域时，该绑定会被清除

### 3.4 与 `{kp-N}` 的关系

在绑定模式下，核心目标是“按客户端 IP 固定出口身份”。

因此对于绑定生成的 `{sid-N}` / `{did-N}`：

- 它会优先保持和来源 IP 的绑定关系
- 不再按 `{kp-N}` 的超时去轮换该绑定值

也就是说，绑定模式更强调“固定映射”，而不是“定时换号”。

实际使用上，建议需要按来源 IP 固定的 Socks5 出站不要再配置 `{kp-N}`。

### 3.5 出站国家验证缓冲池

对于带 `{sid-N}` / `{did-N}` 的 socks 出站，如果 outbound tag 可以按下面规则解析出国家：

```text
US_OUT -> US
JP_OUT -> JP
```

系统会为该出站维护一个已验证账号缓冲池。国家解析规则是取第一个 `_` 前面的两位大写字母；不符合该格式的 tag 不启用验证池。

后台补池流程为：

1. 基于 socks 出站账号模板生成一组新的用户名/密码
2. 用这组用户名/密码通过该 socks 网关访问 `https://gitlab.520531.xyz/get-code`
3. 读取返回 JSON 里的 `country`、`verify`、`block`
4. 如果 `block` 为 `true`，则该出站池进入阻断状态，后续用户会拿到随机无效用户名/密码，让 socks5 认证失败
5. 如果 `verify` 为 `true`，且 `country` 与 outbound tag 解析出的国家一致，则放入该出站的缓冲池
6. 如果 `verify` 为 `false`，则跳过国家匹配并放入该出站的缓冲池
7. 如果验证不通过，则丢弃并继续尝试生成下一组

旧版本验证接口没有 `verify` / `block` 字段时，默认按下面规则处理：

```json
{
  "verify": true,
  "block": false
}
```

注意：socks 服务器地址只是接入网关，不代表真实落地国家。真实国家只以验证接口返回的 `country` 为准。

当新来源 IP 首次命中某个动态 socks 出站时：

- 如果该出站缓冲池里有已验证账号，会优先取一组账号并绑定给该来源 IP
- 如果缓冲池为空或后台验证来不及，会立即生成一组未验证账号给该来源 IP 使用，不阻塞用户请求
- 之后同一来源 IP 再次命中同一出站作用域时，会继续复用已绑定账号，直到该绑定 5 小时未访问后被清理

## 4. 示例

### 4.1 普通动态出口

没有入站来源 IP 上下文，或者出站不是 socks 时：

```json
{
  "protocol": "socks",
  "settings": {
    "servers": [
      {
        "address": "1.2.3.4",
        "port": 1080,
        "users": [
          {
            "user": "brd-zone-us-session-{sid-8}",
            "pass": "secret"
          }
        ]
      }
    ]
  }
}
```

效果：

- 每次握手都可能生成新的 `{sid}`

### 4.2 使用 `{kp-N}` 的普通动态出口

```json
{
  "user": "brd-zone-us-session-{sid-8}{kp-60}",
  "pass": "secret"
}
```

效果：

- 60 秒内复用同一个动态 ID
- 超时后重新生成

### 4.3 来源 IP 固定出口

前提：

- 路由命中 socks 出站
- socks 用户名中配置了 `{sid-N}` 或 `{did-N}`
- 入站上下文中能拿到客户端来源 IP

示例模板：

```json
{
  "user": "brd-zone-us-session-{sid-8}",
  "pass": "secret"
}
```

效果：

- 客户端 A，来源 IP 为 `10.0.0.2`，第一次连接后分配到一个固定 `{sid}`
- 客户端 A 后续再次连接，只要来源 IP 不变，仍使用同一个 `{sid}`
- 客户端 B，来源 IP 为 `10.0.0.3`，会分配到不同的 `{sid}`

### 4.4 不同出站互相隔离

假设有两条路由：

```text
入站 1 -> 出站 1
入站 2 -> 出站 2
```

如果同一个客户端来源 IP 同时访问这两个入站：

- 命中出站 1 时，会使用“来源 IP + 出站 1”绑定出来的用户名/密码
- 命中出站 2 时，会使用“来源 IP + 出站 2”绑定出来的用户名/密码
- 两个出站的动态用户名/密码不会因为来源 IP 相同而混用

## 5. 不会破坏的原有逻辑

当前实现保持了下面这些兼容性：

- 非 socks 出站不受影响
- 不包含 `{sid-N}` / `{did-N}` 的 socks 账号不受影响
- 例如 `FREE_IN -> free_out` 这类 `freedom` 出站没有动态模板，不会提取国家、不会验证、不会建池、不会写入用户绑定缓存
- 原有 `{kp-N}` 普通动态缓存逻辑仍然保留
- 绑定逻辑不会修改共享配置对象，而是在单次请求上下文中生成当前连接使用的账号

## 6. 当前限制

### 6.1 缓存为进程内内存

当前来源 IP 到动态 ID 的映射，以及已验证账号缓冲池，都保存在当前进程内存中。

这意味着：

- 同一进程生命周期内可复用
- 服务重启后映射会丢失
- 重启后同一个 IP 可能重新分配新的动态 ID
- 某个来源 IP 的绑定如果 5 小时没有再次访问，会被后台清理

### 6.2 来源 IP 是“直连到当前 Xray 的对端 IP”

对于 `vmess`、`vless`、`trojan`、`http`、`socks` 等正常 TCP/UDP 入站，`inbound.Source` 一般就是对端连接 IP。

如果前面还有反向代理、四层转发、网关或其他中间层，那么这里看到的可能是中间层 IP，而不是最终用户公网 IP。

## 7. 建议用法

如果你的目标是“一个来源 IP 固定一个住宅出口”，建议：

1. 将相关流量路由到 socks 出站
2. 在 socks 出站用户名或密码中使用 `{sid-N}` 或 `{did-N}`
3. 不要在需要来源 IP 固定的模板里继续使用 `{kp-N}`

推荐模板示例：

```text
username: brd-customer-xxx-zone-us-session-{sid-8}
password: your-password
```

或：

```text
username: brd-customer-xxx-zone-us-session-{did-10}
password: your-password
```
